<h1><%= @list.id ? 'Edit List' : 'Create List' %></h1>
<hr />
<%= form_with(model: [@board, @list], local: true, class: "ui form") do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name,required: true, autofocus: true %>
  <%= f.subit nil, class: "ui button" %>