<h1 class="ui header">My To-Do Lists</h1>
<ul>
<% @boards.each do |board| %>
  <li>
<%= board.name%>
<br />
  <%= link_to "Show", board_path(board) %>
  <%= link_to "Edit", edit_board_path(board)%>
  <%= link_to "Destroy", board_path(board), method: :delete%>
  </li>
<% end %>
</ul>
<br />
<%= link_to "New Board", new_board_path %>